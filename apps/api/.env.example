# Firebase Admin SDK (server-side only — from a service account on the
# WeightMaster Firebase project, NEVER commit real values or reuse another
# project's credentials)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY_ID=
FIREBASE_CLIENT_ID=
FIREBASE_PRIVATE_KEY=

PORT=3000
